# v3_aug: Phase A + B (latent augmentation enabled)
# Merged on top of: base.yaml -> train_meanflow.yaml -> picasso/train_meanflow.yaml

paths:
  checkpoints_dir: "${paths.results_root}/ablations/v3_aug/checkpoints"
  logs_dir: "${paths.results_root}/ablations/v3_aug/logs"
  samples_dir: "${paths.results_root}/ablations/v3_aug/samples"
  diagnostics_dir: "${paths.results_root}/ablations/v3_aug/diagnostics"

training:
  augmentation:
    enabled: true

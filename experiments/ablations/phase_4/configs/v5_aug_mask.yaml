# v5_aug_mask: Phase A + B + C (augmentation + spatial masking)
# Merged on top of: base.yaml -> train_meanflow.yaml -> picasso/train_meanflow.yaml

paths:
  checkpoints_dir: "${paths.results_root}/ablations/v5_aug_mask/checkpoints"
  logs_dir: "${paths.results_root}/ablations/v5_aug_mask/logs"
  samples_dir: "${paths.results_root}/ablations/v5_aug_mask/samples"
  diagnostics_dir: "${paths.results_root}/ablations/v5_aug_mask/diagnostics"

training:
  augmentation:
    enabled: true

meanflow:
  spatial_mask_ratio: 0.5

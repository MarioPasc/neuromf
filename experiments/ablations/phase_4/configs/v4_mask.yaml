# v4_mask: Phase A + C (spatial masking at 50%)
# Merged on top of: base.yaml -> train_meanflow.yaml -> picasso/train_meanflow.yaml

paths:
  checkpoints_dir: "${paths.results_root}/ablations/v4_mask/checkpoints"
  logs_dir: "${paths.results_root}/ablations/v4_mask/logs"
  samples_dir: "${paths.results_root}/ablations/v4_mask/samples"
  diagnostics_dir: "${paths.results_root}/ablations/v4_mask/diagnostics"

meanflow:
  spatial_mask_ratio: 0.5

"""Data loading: MRI preprocessing, latent datasets, toroid, and FCD.

Provides NIfTI-to-tensor preprocessing, pre-computed latent datasets, synthetic
toroidal manifold data for pipeline validation, and FCD image-mask pair loading.
"""
